#include "main.h"
#include <string.h>
/**
 * _strchr - C program to locate a character in a string
 * @s: string
 * @c: character
 * Return: character
 */

char *_strchr(char *s, char c)
{
	return (strchr(s, c));
}
