#include "main.h"
#include <string.h>
/**
 * _strpbrk - C program to search a string for any of a set of byte
 * @s: string
 * @accept: strin
 * Return: byte
 */

char *_strpbrk(char *s, char *accept)
{
	return (strpbrk(s, accept));
}
