#include "main.h"
#include <stdio.h>
/**
 * mul -  multiplies two integers
 * @a: an integer
 * @b: another integer
 * Return: multiplication
 */

int mul(int a, int b)
{
	int mult;

	mult = a * b;
	printf("%d", mult);
	return (0);
}
