#include "main.h"
#include <stdio.h>
/**
 * mul -  multiplies two integers
 * @a: an integer
 * @b: another integer
 * Return: multiplication
 */

int mul(int a, int b)
{
	int mult = 0;

	mult = a * b;
	printf("%d", mult);
	return (mult);
}

	int main(void)
{

		int result = mult(int a, int b);

		printf("%d, result);
		return (0);
}
