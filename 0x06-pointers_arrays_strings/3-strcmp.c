#include "main.h"
#include <string.h>
/**
 * _strcmp - C program to compare two strings
 * @s1: pointer to a string
 * @s2: pointer to a string
 * Return: 0, positive or negative value
 */

int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
