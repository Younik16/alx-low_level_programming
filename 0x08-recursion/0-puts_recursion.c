#include "main.h"
#include <stdio.h>
/**
 * _puts_recursion - program to print a string followed by a new line
 * @s: pointer to string
 * Return: nothing
 */

void _puts_recursion(char *s)
{
	puts(s);
}
