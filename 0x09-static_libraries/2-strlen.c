#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen - C program to check for length of string
 * @s: pointer function parameter
 * Return: length
 */

int _strlen(char *s)
{
	int length = strlen(s);

	return (length);
}
